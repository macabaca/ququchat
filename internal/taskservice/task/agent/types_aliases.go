package agent

import agenttypes "ququchat/internal/taskservice/task/agent/types"

type ToolSpec = agenttypes.ToolSpec
type SchemaField = agenttypes.SchemaField
type CoordinatorSchemaConfig = agenttypes.CoordinatorSchemaConfig
type AgentIdentityConfig = agenttypes.AgentIdentityConfig
type Input = agenttypes.Input

type plannerTask = agenttypes.PlannerTask
type plannerOutline = agenttypes.PlannerOutline

type action = agenttypes.Action
type plan = agenttypes.Plan
type validationIssue = agenttypes.ValidationIssue
type finalReviewResult = agenttypes.FinalReviewResult
type DomainState = agenttypes.DomainState
type ControlState = agenttypes.ControlState
type State = agenttypes.State
